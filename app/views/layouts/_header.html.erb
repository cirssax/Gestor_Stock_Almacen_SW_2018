<header class="navbar navbar-fixed-top navbar-inverse">
  <div class="navbar-inner">
    <div class="container">
      <div class="navbar-header">
      </div>
      <nav class="navbar-collapse bs-navbar-collapse collapse">
        <ul class="nav navbar-nav navbar-right">
          <% if signed_in? %>
            <li class="dropdown">
              <a href="#" class="dropdown-toggle" data-toggle="dropdown"> Account <b class="caret"></b></a>
              <ul class="dropdown-menu" role="menu">
                <li><%= link_to "Profile", "#" %></li>
                <li class="divider"></li>
                <li> <%= link_to 'Salir', destroy_user_session_path,:class =>"btn btn-primary btn-sm", method: :delete %></li>
              </ul>
            </li>
          <% else %>
            <div>
              <%= link_to 'Inicial Sesion', new_user_session_path,:class=>"btn btn-primary"%>
              <%= link_to 'Registrarse', new_user_registration_path,:class=>"btn btn-primary " %>
            </div>
          <% end %>
        </ul>
      </nav>
    </div>
  </div>
</header>